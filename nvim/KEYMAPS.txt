#TODO: 

                                        -- NVIM KEYMAP TRACKER --

-- MODIFIER: <LEADER>

    --MODE: <NORMAL>

        -- NEOTREE
        <t> = toggle tree on left sidebar

        -- TELESCOPE
        <pf> = open telescope project files
        <gf> = open telescope git files
        <ps> = grep search for string
        
        -- HARPOON
        <e> = toggle harpoon gui
        <a> = add buffer to harpoon
        <1> = navigate to file 1
        <2> = navigate to file 2
        <3> = navigate to file 3

        -- FUGITIVE
        <gs> = open git menu

        -- LSP NAVIGATION & ACTIONS
        <rn> = rename symbol under cursor
        <ca> = code action (normal and visual)
        <vd> = show diagnostic in floating window
        <dl> = send diagnostics to location list
        <ds> = document symbols (outline)
        <ws> = workspace symbol search
        <wa> = add workspace folder
        <wr> = remove workspace folder
        <wl> = list workspace folders
        <ci> = incoming calls (who calls this function)
        <co> = outgoing calls (what does this function call)
        <sh> = signature help

        -- RUST-SPECIFIC (rustaceanvim)
        <rm> = expand macro recursively
        <rp> = go to parent module
        <rc> = open Cargo.toml
        <rj> = smart join lines
        <rh> = hover actions (Rust-enhanced)
        <re> = explain error under cursor
        <rd> = render full diagnostic
        <rr> = run runnables (tests, binaries)
        <rb> = run debuggables

        -- GENERAL
        <.> = open netrw
        <f> = format selected text
        <s> = replace/edit/modify current word across the whole buffer
        <x> = add user permission to execute in current repository
        <y> = copy selection to system buffer
        <p> = paste without overwriting register
        <d> = delete without overwriting register
        <k> = navigate to next location list item
        <j> = navigate to previous location list item


    --MODE: <VISUAL>

    --MODE: <INSERT>

    --MODE: <COMMAND>



-- MODIFIER: <CONTROL>

    --MODE: <NORMAL>

    --MODE: <VISUAL>

    --MODE: <INSERT>
        <C-s> = signature help (in insert mode)

    --MODE: <COMMAND>



-- MODIFIER: <SHIFT>

    --MODE: <NORMAL>

    --MODE: <VISUAL>

    --MODE: <INSERT>

    --MODE: <COMMAND>



-- MODIFIER: <NONE>

    --MODE: <NORMAL>
        <j> = cursor down
        <k> = cursor up
        <h> = cursor left
        <l> = cursor right
        <p> = paste
        <K> = LSP hover documentation

        -- LSP CODE NAVIGATION (no modifier)
        <gd> = go to definition
        <gD> = go to declaration
        <gi> = go to implementation
        <gt> = go to type definition
        <gr> = list all references
        <]d> = next diagnostic
        <[d> = previous diagnostic

    --MODE: <VISUAL>
        <y> = yank (copy)
        <p> = paste
        <>

    --MODE: <INSERT>

    --MODE: <COMMAND>



-- MISC:

    --NEOTREE KEYBINDINGS

        --GLOBAL
        <A> = add directory 
        <d> = delete
        <r> = rename
        <m> = move
        <i> = show file details
        <?> = show help:w

        <q> = close window
        <y> = copy to clipboard
        <x> = cut to clipboard
        <p> = paste from clipboard
        
        <<> = show previous source
        <>> = show next source

        <z> = close all nodes
        <Z> = open all nodes
        <R> = refresh

        --FILESYSTEM

        --BUFFERS

        --GIT

        
        <> = 
        <> = 
        <> = 
        <> = 
        <> =
        <> = 

        <> = 
        <> = 
        <> =
        <> = 


    --GLOBAL LUA FUNCTIONS (:lua <function>)
        <SetColors()> = update colorscheme application for nvim
