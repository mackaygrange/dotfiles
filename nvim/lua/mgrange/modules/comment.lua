return
{
  'numToStr/Comment.nvim',
  opts = {
    -- add any options here; default keymaps like `gcc` (line) and `gc` (visual) work out of the box
  },
  lazy = false, -- or set an event like "BufEnter" or "VeryLazy" for lazy loading
}

